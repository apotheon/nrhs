<h2>Manage Categories</h2>

<ul>
  <% @categories.each do |category| %>
    <li><%= link_to "view #{category.name}", category_path(category) %></li>
  <% end %>
</ul>

<%= link_to 'New Category', new_category_path %>
