<nav class="nav main-nav">
  <ul class="navbar">
    <% get_active_categories.each do |category| %>
      <li>
        <%= link_to category.name, category_path(category) %>
        <ul>
          <% category.sorted_pages.each do |page| %>
            <li><%= link_to page.title, page_path(page) %></li>
          <% end %>
        </ul>
      </li>
    <% end %>
  </ul>
</nav>
